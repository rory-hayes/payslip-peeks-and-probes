# Payslip Insights production-readiness audit

Generated: 2026-08-25T18:36:48+00:00  
Project: `/Users/rory/Documents/Ideation/payslip-insights`  
Reviewed revision: `b8a96135a3604d94b7a5051123fd6698b4ec393b`  
Branch: `agent/payslip-insights-full-build`  
Working tree at audit start: clean

## Executive verdict

Customer launch status: **not ready**.  
Overall customer-readiness score: **4/10**.

The reviewed Payslip code is in good local shape, but the public domain is
still serving an older deployment and the intended Supabase project is missing
seven required functions. Therefore upload, original-document access,
checkout-return verification, and account deletion cannot be accepted as live
customer flows.

No real customer account, payslip, purchase, webhook, email, or production
mutation was performed during this audit.

## Scorecard

| Area | Score | Evidence and remaining gap |
| --- | ---: | --- |
| Source, build, and tests | 9/10 | `npm run verify` passes: typecheck, lint, Edge Function contract, Sites tests, deployment-helper tests, 69 test files/332 tests, and production build. Lint has 9 existing Fast Refresh warnings and the build reports stale Browserslist data. |
| Local UX and responsive routes | 9/10 | Rendered desktop and 390px mobile checks passed for the landing/demo/preview path, route metadata, no horizontal overflow, and no browser console errors. A physical-device/native release was not verified. |
| Branding and public source hygiene | 9/10 locally | The current build has the Payslip mark, title, favicon assets, and no user-facing old editor badge. Internal detector tests and historical reports retain provider-name text intentionally to prevent regressions. |
| AI extraction | 7/10 | Server-side Vercel AI Gateway request, strict schema, bounded parser, evidence, context, YTD, line items, and mandatory human review are implemented. Accuracy on real redacted fixtures, target key/configuration, provider retention, and line-item correction persistence remain unproven. |
| Stripe implementation | 7/10 in source; 2/10 live proof | Server-owned catalog, environment handshake, embedded Checkout, idempotency, exact-session return verification, webhook-only entitlement, refund/reconciliation paths, and Customer Portal are implemented and tested locally. The target deployment is missing `verify-checkout-return`; no sandbox transaction or webhook has been exercised. |
| Supabase release | 3/10 | `process-payslip`, `create-checkout`, `payments-webhook`, and `create-portal-session` respond, but seven required routes return HTTP 404. Migrations, secrets, workers, RLS/storage isolation, and deletion races are not live-proven. |
| Public domain | 1/10 | `https://payslipinsights.com` returns HTTP 200 but serves the older shell, old title, public editor badge, host-injected analytics, and no `/release.json`; it does not match the reviewed revision. |
| Legal and operations | 3/10 | Privacy and Terms still intentionally block paid preflight until the operating entity, governing law, provider/retention/analytics/cookie disclosures, and support details are supplied and approved. |

## Confirmed blockers

### 1. Public domain is not the reviewed Payslip release

The public verifier against revision `b8a9613` reports:

- `/release.json` is HTTP 404;
- the title is from the retired shell;
- the retired payroll-mistakes claim remains;
- a public editor badge remains;
- host-injected analytics sit outside the app consent flow;
- route-specific canonical, Open Graph, social-image, structured-data, and
  signed-out noindex metadata do not match the reviewed build.

The exact reviewed commit is present on both remote `main` and
`agent/payslip-insights-full-build`, but the domain has not picked it up.

### 2. Intended Supabase project is incomplete

Target project: `shvivlhawhczbljzhvmr`.

Current route verification:

| Function | Result |
| --- | --- |
| `start-payslip-upload` | HTTP 404 |
| `finish-payslip-upload` | HTTP 404 |
| `get-payslip-original-url` | HTTP 404 |
| `delete-failed-payslip` | HTTP 404 |
| `cleanup-expired-payslip-uploads` | HTTP 404 |
| `process-payslip` | HTTP 200 |
| `create-checkout` | HTTP 200 |
| `verify-checkout-return` | HTTP 404 |
| `payments-webhook` | HTTP 405, route present |
| `create-portal-session` | HTTP 200 |
| `delete-account` | HTTP 404 |

The guarded deployment script is ready, but the current Supabase CLI identity
does not own this project and no local database password is configured. The
deployment must not be guessed or partially run.

### 3. Legal/provider facts are still required

The paid release preflight correctly fails until the owner supplies:

- legal operating entity and support/contact details;
- governing law and applicable consumer wording;
- actual AI/payment/analytics providers and data-processing terms;
- original-document, provider, and deletion-billing-review retention rules;
- cookie/analytics disclosure and consent decision.

These facts cannot be safely invented in source code.

### 4. AI quality is implemented but not yet demonstrated as accurate

The extraction path is a strong, fail-closed first-pass transcription workflow,
not yet a verified accuracy product. Before accepting customer payslips it
needs owner-approved redacted UK/Ireland fixtures, human ground truth, field and
line-item error metrics, PDF/image/provider-error/retry/isolation runs, and a
confirmed target `AI_GATEWAY_API_KEY`. Detailed line items are currently
source evidence; headline fields can be corrected and confirmed, but there is
no separate persisted line-item correction ledger or AI Q&A workflow.

## Verification performed

| Check | Result |
| --- | --- |
| `npm run verify` | Pass |
| `npm run release:web:preflight -- --paid` | Correctly blocked by legal/provider placeholders and manual live proof requirements |
| `npm run release:web:verify-public -- --url https://payslipinsights.com --revision b8a96135a3604d94b7a5051123fd6698b4ec393b` | Fail, confirming the domain is stale |
| `npm run verify:supabase-deployment` | Fail, confirming seven missing target functions |
| Desktop rendered smoke | Pass locally for landing, demo, preview, pricing, guides, legal, and signed-out routes |
| 390px rendered smoke | Pass locally for landing, demo, preview, route overflow, and console-error checks |
| Real sign-up, upload, payslip provider call, purchase, webhook, deletion, and customer email | Not run; no authorization/credentials/test accounts supplied |

## Exact path to 10/10

1. Sign into the owner Lovable account in the waiting browser handoff and
   connect/publish the reviewed GitHub `main` revision to the project serving
   `payslipinsights.com`. Confirm the domain points to that published release.
2. Sign into the Supabase account that owns `shvivlhawhczbljzhvmr`, set the
   database password only in the local shell, apply the guarded migrations, and
   deploy every function from the reviewed revision. Then rerun the route
   verifier.
3. Configure the server-only AI Gateway and Stripe secrets in the target
   project, matching the browser Stripe environment and all six catalog prices.
4. Supply and approve the legal, provider, retention, analytics, cookie, and
   support facts so paid preflight can pass.
5. Run two-account authenticated acceptance, redacted AI fixtures, storage and
   deletion race tests, and the complete Stripe sandbox flow. A live charge
   still requires explicit owner approval.
6. Re-run the public verifier, live route checks, and the full local suite after
   the final deployment. Only then should the site be described as ready for
   customers.
