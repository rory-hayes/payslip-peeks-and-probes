/**
 * The compact 180px mark is intentionally shared by the favicon and visible
 * brand lockups. The source artwork is much larger than any rendered mark and
 * would add more than 400KB to the first application download.
 */
export const BRAND_MARK_PATH = '/favicon.png?v=payslip-insights-1';
